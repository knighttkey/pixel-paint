import React, {
  DragEvent,
  useRef,
  useState,
  MouseEvent,
  useEffect
} from "react";
import "./../styles/Template.scss";
import * as R from "ramda";

type Props = {
};

export default () => {

  return (
    <div className="_container">
  
    </div>
  );
};
